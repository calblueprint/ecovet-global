export default function RoleForm() {
  return (
    <div>
        <fieldset>
            <legend>Role Description</legend>
            <input type="text" name="role_description" placeholder="da descrition" />
        </fieldset>
    </div>
  );
}